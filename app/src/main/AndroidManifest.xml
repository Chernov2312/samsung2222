<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lesson17032023"
        tools:targetApi="31">
        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".Character_Settings" />
        <activity android:name=".InventoryActivity" />
        <activity android:name=".startgame" />
        <activity android:name=".Home" />
        <activity android:name=".Gorod" />
        <activity android:name=".magazin" />
        <activity android:name=".Buyproduct" />
        <activity android:name=".rasxodn" />
        <activity android:name=".gildia" />
        <activity android:name=".BossFight" />
        <activity android:name=".GameLose" />
    </application>
</manifest>